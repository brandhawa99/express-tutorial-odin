extends layout

block content
    h1 #{title}: #{book.title}
    
    p Do you really want to delet this book?


    form(method='POST' action='')
      div.form-group
        input#genreid.form-control(type='hidden',name='bookid', required='true', value=book._id )

      button.btn.btn-primary(type='submit') Delete
